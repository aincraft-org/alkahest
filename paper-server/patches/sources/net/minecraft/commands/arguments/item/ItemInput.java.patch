--- a/net/minecraft/commands/arguments/item/ItemInput.java
+++ b/net/minecraft/commands/arguments/item/ItemInput.java
@@ -9,8 +_,15 @@
 import net.minecraft.network.chat.Component;
 import net.minecraft.world.item.Item;
 import net.minecraft.world.item.ItemStack;
+import org.jetbrains.annotations.Nullable;
 
-public record ItemInput(Holder<Item> item, DataComponentPatch components) {
+public record ItemInput(
+    Holder<Item> item,
+    DataComponentPatch components,
+    // mintychochip start - custom Material / custom-block identity for /give
+    @Nullable dev.mintychochip.customblock.CustomBlockDefinition mintyCustom
+    // mintychochip end - custom Material / custom-block identity for /give
+) {
     private static final Dynamic2CommandExceptionType ERROR_STACK_TOO_BIG = new Dynamic2CommandExceptionType(
         (item, count) -> Component.translatableEscape("arguments.item.overstacked", item, count)
     );
@@ -18,7 +_,23 @@
         error -> Component.translatableEscape("arguments.item.malformed", error)
     );
 
+    /** Vanilla / component-only item (no custom-block stamp). */
+    public ItemInput(final Holder<Item> item, final DataComponentPatch components) {
+        this(item, components, null);
+    }
+
     public ItemStack createItemStack(final int count) throws CommandSyntaxException {
+        // mintychochip start - custom Material stacks for /give
+        if (this.mintyCustom != null) {
+            final org.bukkit.inventory.ItemStack bukkit =
+                dev.mintychochip.customblock.CustomBlocks.createItemStack(this.mintyCustom, Math.max(1, count));
+            final ItemStack nms = org.bukkit.craftbukkit.inventory.CraftItemStack.asNMSCopy(bukkit);
+            if (count > nms.getMaxStackSize()) {
+                throw ERROR_STACK_TOO_BIG.create(this.mintyCustom.namespacedKey().toString(), nms.getMaxStackSize());
+            }
+            return nms;
+        }
+        // mintychochip end - custom Material stacks for /give
         ItemStack result = new ItemStack(this.item, count, this.components);
         if (count > result.getMaxStackSize()) {
             throw ERROR_STACK_TOO_BIG.create(this.item.getRegisteredName(), result.getMaxStackSize());
