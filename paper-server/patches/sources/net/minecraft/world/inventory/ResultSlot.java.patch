--- a/net/minecraft/world/inventory/ResultSlot.java
+++ b/net/minecraft/world/inventory/ResultSlot.java
@@ -39,6 +_,11 @@
     protected void onQuickCraft(final ItemStack picked, final int count) {
         this.removeCount += count;
         this.checkTakeAchievements(picked);
+        // Paper start - add ItemCraftedEvent
+        if (!picked.isEmpty()) {
+            new io.papermc.paper.event.inventory.ItemCraftedEvent((org.bukkit.entity.Player) this.player.getBukkitEntity(), org.bukkit.craftbukkit.inventory.CraftItemStack.asBukkitCopy(picked)).callEvent();
+        }
+        // Paper end - add ItemCraftedEvent
     }
 
     @Override
@@ -79,14 +_,52 @@
     private NonNullList<ItemStack> getRemainingItems(final CraftingInput input, final Level level) {
         return level instanceof ServerLevel serverLevel
             ? serverLevel.recipeAccess()
-                .getRecipeFor(RecipeType.CRAFTING, input, serverLevel)
+                .getRecipeFor(RecipeType.CRAFTING, input, serverLevel, this.craftSlots.getCurrentRecipe()) // Paper - Perf: Improve mass crafting; check last recipe used first
                 .map(recipe -> recipe.value().getRemainingItems(input))
-                .orElseGet(() -> copyAllInputItems(input))
+                .orElseGet(() -> CraftingRecipe.defaultCraftingReminder(input)) // Paper - Fix #1822: Return default remainder when recipe is null
             : CraftingRecipe.defaultCraftingReminder(input);
     }
 
+    /**
+     * Stamp the craft result with {@code CRAFT} + ingredient parents.
+     * Must run on the live result stack <em>before</em> shift-click moves it into
+     * the inventory; after a successful quick-move the stack is often empty and
+     * {@link #onTake} would otherwise never see the items that landed in the bag.
+     */
+    // mintychochip start - item provenance: craft stamp helper (shift-click + onTake)
+    public void stampCraftResult(final Player player, final ItemStack result) {
+        if (result.isEmpty()) {
+            return;
+        }
+        final java.util.List<java.util.UUID> parents = this.craftParentIds();
+        final dev.mintychochip.provenance.StackLocation craftLocation =
+            dev.mintychochip.provenance.StackLocation.playerSlot(player.getUUID(), -1);
+        dev.mintychochip.provenance.ItemProvenance.onCrafted(result, parents, craftLocation);
+    }
+
+    private java.util.List<java.util.UUID> craftParentIds() {
+        final CraftingInput.Positioned positionedRecipe = this.craftSlots.asPositionedCraftInput();
+        final CraftingInput input = positionedRecipe.input();
+        final int recipeLeft = positionedRecipe.left();
+        final int recipeTop = positionedRecipe.top();
+        final java.util.List<ItemStack> ingredientSnapshot = new java.util.ArrayList<>();
+        for (int y = 0; y < input.height(); y++) {
+            for (int x = 0; x < input.width(); x++) {
+                final int slot = x + recipeLeft + (y + recipeTop) * this.craftSlots.getWidth();
+                ingredientSnapshot.add(this.craftSlots.getItem(slot));
+            }
+        }
+        return dev.mintychochip.provenance.ItemProvenance.collectParents(ingredientSnapshot);
+    }
+    // mintychochip end - item provenance
+
     @Override
     public void onTake(final Player player, final ItemStack carried) {
+        // Paper start - add ItemCraftedEvent
+        if (!carried.isEmpty()) {
+            new io.papermc.paper.event.inventory.ItemCraftedEvent((org.bukkit.entity.Player) player.getBukkitEntity(), org.bukkit.craftbukkit.inventory.CraftItemStack.asBukkitCopy(carried)).callEvent();
+        }
+        // Paper end - add ItemCraftedEvent
         this.checkTakeAchievements(carried);
         CraftingInput.Positioned positionedRecipe = this.craftSlots.asPositionedCraftInput();
         CraftingInput input = positionedRecipe.input();
@@ -94,17 +_,64 @@
         int recipeTop = positionedRecipe.top();
         NonNullList<ItemStack> remaining = this.getRemainingItems(input, player.level());
 
+        // mintychochip start - item provenance: craft transform + ingredient consume
+        // Stamp may already have been applied before shift-click moveItemStackTo.
+        final java.util.List<java.util.UUID> parents = this.craftParentIds();
+        final dev.mintychochip.provenance.StackLocation craftLocation =
+            dev.mintychochip.provenance.StackLocation.playerSlot(player.getUUID(), -1);
+        if (!carried.isEmpty()) {
+            dev.mintychochip.provenance.ItemProvenance.onCrafted(carried, parents, craftLocation);
+        }
+        // mintychochip end - item provenance
+
         for (int y = 0; y < input.height(); y++) {
             for (int x = 0; x < input.width(); x++) {
                 int slot = x + recipeLeft + (y + recipeTop) * this.craftSlots.getWidth();
                 ItemStack itemStack = this.craftSlots.getItem(slot);
                 ItemStack replacement = remaining.get(x + y * input.width());
+                // mintychochip start - item provenance: ingredient consume + remainder parent
+                java.util.Optional<java.util.UUID> consumedId = java.util.Optional.empty();
+                // mintychochip end - item provenance
                 if (!itemStack.isEmpty()) {
+                    // mintychochip start - item provenance: track ingredient consume
+                    consumedId = dev.mintychochip.provenance.StackStamp.readId(itemStack);
                     this.craftSlots.removeItem(slot, 1);
                     itemStack = this.craftSlots.getItem(slot);
+                    if (itemStack.isEmpty()) {
+                        consumedId.ifPresent(id ->
+                            dev.mintychochip.provenance.ItemProvenance.death(
+                                id,
+                                dev.mintychochip.provenance.ProvenanceReason.CONSUMED,
+                                null
+                            )
+                        );
+                    } else {
+                        dev.mintychochip.provenance.ItemProvenance.noteConsumed(itemStack);
+                    }
+                    // mintychochip end - item provenance
                 }
 
                 if (!replacement.isEmpty()) {
+                    // mintychochip start - item provenance: craft remainder (empty bucket/bottle)
+                    final java.util.List<java.util.UUID> remainderParents = new java.util.ArrayList<>();
+                    consumedId.ifPresent(remainderParents::add);
+                    if (remainderParents.isEmpty()) {
+                        remainderParents.addAll(parents);
+                    }
+                    if (!remainderParents.isEmpty()) {
+                        dev.mintychochip.provenance.ItemProvenance.onIdentityHandoff(
+                            replacement,
+                            remainderParents,
+                            craftLocation
+                        );
+                    } else {
+                        dev.mintychochip.provenance.ItemProvenance.birthIfAbsent(
+                            replacement,
+                            dev.mintychochip.provenance.ProvenanceSource.SPECIAL_RECIPE,
+                            craftLocation
+                        );
+                    }
+                    // mintychochip end - item provenance
                     if (itemStack.isEmpty()) {
                         this.craftSlots.setItem(slot, replacement);
                     } else if (ItemStack.isSameItemSameComponents(itemStack, replacement)) {
